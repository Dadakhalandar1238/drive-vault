services:
  - type: web
    name: drive-vault
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GOOGLE_PICKER_API_KEY
        sync: false
      - key: MAX_DRIVES_PER_USER
        value: "10"
      - key: MAX_WORKERS
        value: "4"
